# Deploy from this repository: only the `site/` folder goes to cPanel.
#
# Recommended: `npm run package:cpanel` then upload `dist-cpanel-php/`.
#
# Or rsync only `site/`:
#   rsync -av --delete --exclude-from=deploy/cpanel-site-excludes.txt site/ user@host:public_html/

.git/
.cursor/
node_modules/
dist-cpanel-php/
